// Parvez Ali, ICE-13 of PUST
#include <bits/stdc++.h>
using namespace std;

typedef long long ll;
typedef vector<int> vint;
typedef vector<string> vstr;
typedef vector<ll> vll;
#define endl "\n"
#define IOS ios_base::sync_with_stdio(false);cin.tie(NULL)
#define mod (1e9+7)

int main(){
    IOS;
    int t;
    cin>>t;
    while (t--)
    {
        int n;
        cin>>n;
        string s;
        cin>>s;
        int a=0,b=0;
        for (int i = 0; i < n-1; i++)
        {   
            for (int j = 1; j < n; j++)
            {
                if (s[i]==s[j]&&j>b)
                {
                    a=i;
                    b=j;
                }
                else{break;
            }
            string ss = s.substr(a,b);
            for (size_t i = 0; i < count; i++)
            {
                sd tfvsadfcudwuo
            }
        }
    }
    return 0;
}