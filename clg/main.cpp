#include <bits/stdc++.h>
using namespace std;

void solution(int &T) { 
	// pirnt("{}", "HI THERE");
	cout<<"There is nothing than the most used material in this scope for the variable in the scope for the msot of mine things on the vaariable of the "<<endl;
}

signed main() {
  int T;
  // cin >> T;
  solution(T);
  return 0;
}